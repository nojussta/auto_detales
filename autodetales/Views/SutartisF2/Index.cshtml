@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.Models.SutartisF2.SutartisL>

@* this will also be used as page title in main layout *@
@{ ViewData["title"] = "Skrydžiai (F2)"; }

@* content header *@
<div class="main-content-header">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

@* content itself *@
<div class="main-content-lst">
	<table class="entity-list">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.Nr)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Data)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Klientas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Vieta)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Uzsakymo_Nr)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.Nr)
				</td>
				<td>
					@Html.DisplayFor(m => item.Data)
				</td>
				<td>
					@Html.DisplayFor(m => item.Klientas)
				</td>
				<td>
					@Html.DisplayFor(m => item.Vieta)
				</td>
				<td>
					@Html.DisplayFor(m => item.Uzsakymo_Nr)
				</td>
				<td>
					<a 
						href="@Url.Action("Edit", new { id = item.Nr })" 
						class="btn btn-primary"
						><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
						
					<a 
						href="@Url.Action("Delete", new { id = item.Nr })" 
						class="btn btn-danger"
						><i class="fa-solid fa-trash"></i> Šalinti</a>					
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>